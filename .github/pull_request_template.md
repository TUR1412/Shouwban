## 变更说明
- 

## 关联 Issue（可选）
- 

## 自检清单
- [ ] 已同步更新缓存版本号（HTML `?v=` + `sw.js` `CACHE_NAME`/`PRECACHE_URLS`）
- [ ] 已执行 `pwsh -NoLogo -NoProfile -Command 'node --check scripts/main.js; node --check sw.js'`
- [ ] 已执行 `pwsh -NoLogo -NoProfile -Command 'node scripts/validate.mjs'`
- [ ] 深色/浅色主题均可读（关键组件背景/文字/边框一起更新，避免“浅底浅字/深底深字”）
- [ ] 未在页面主区域裸露 JSON / Error Stack（错误信息已“翻译成人话”）
- [ ] 未引入长驻后台服务（仓库脚本只包含有限任务）

## 截图/录屏（可选）
- 
